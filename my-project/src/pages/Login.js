import React from "react";

export default function Login(){
    return(
       <div  suppressHydrationWarning={true} className="w-screen h-screen bg-blue-400 ">
        {/* {typeof window === 'undefined' ? (
        // Server-side rendering
        <div>Loading...</div>
      ) : (
        // Client-side rendering
        // <div>{data}</div>
      )} */}
       </div>
    );
}